{
  "name": "colloquip-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "demo": "npx playwright test --headed",
    "demo:competition": "npx playwright test demo-competition.spec.ts --headed",
    "demo:competition:mock": "DEMO_MODE=mock npx playwright test demo-competition.spec.ts --headed",
    "demo:vite": "DEMO_BASE_URL=http://localhost:5173 npx playwright test --headed",
    "demo:record": "npx playwright test --headed --video on",
    "install:browsers": "npx playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
